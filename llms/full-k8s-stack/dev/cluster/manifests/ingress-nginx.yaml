# NGINX Ingress Controller for dev
# Installed via Helm with values from values.yaml
# Namespace: ingress-nginx
# Note: If nginx ingress already exists, this will be skipped or updated

# The actual installation is handled via Helm in the deploy.sh script
# This placeholder indicates the intent - see values.yaml for configuration

# Helm install command used:
# helm repo add ingress-nginx https://kubernetes.github.io/ingress-nginx
# helm install ingress-nginx ingress-nginx/ingress-nginx --namespace ingress-nginx --create-namespace -f values.yaml